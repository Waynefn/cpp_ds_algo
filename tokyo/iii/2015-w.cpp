/*
问题2：dijkstra
问题3：fib，任意整数N=fib(n)+M，且M=fib(m)+L,......
		任意整数N由fib数列组成
*/

#include <iostream>
#include <bits/stdc++.h>

using namespace std;

#define Len(x)		sizeof(x)/sizeof(x[0])	
#define PRINT_ARRAY(a,n){for(int i = 0; i < n; i++) cout<<a[i]<<"|"; cout<<endl;}
